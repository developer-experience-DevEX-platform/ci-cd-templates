version: 2
updates:
  # Actions are pinned to commit SHAs in the workflows. Dependabot bumps the
  # SHA and the trailing version comment together.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns:
          - "*"
    commit-message:
      prefix: chore
